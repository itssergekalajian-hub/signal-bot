# Secrets — NEVER commit these
.env

# Telegram session files (contain login tokens for your account + bot)
*.session
*.session-journal

# Open-position ledger (your live holdings — machine-local state)
positions.json
positions.json.tmp

# Python
__pycache__/
*.pyc
venv/
.venv/
